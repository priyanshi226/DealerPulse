GEMINI_API_KEY=your_gemini_api_key_here
# Optional — defaults to gemini-flash-lite-latest (fast, generous free-tier daily quota).
# GEMINI_MODEL=gemini-flash-lite-latest
# Optional — port for the API/static server (server/index.js).
# PORT=8787
